{
  "name": "chit-chat",
  "version": "1.0.0",
  "scripts": {
    "docker": "docker compose -f docker/docker-compose.yml -p chit-chat up",
    "docker:rebuild": "docker compose -f docker/docker-compose.yml -p chit-chat up -V --build"
  }
}
